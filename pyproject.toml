[project]
name = "legal-rag"
version = "0.1.0"
description = "A production-grade RAG system for Indian legal documents"
requires-python = ">=3.12"

[tool.black]
line-length = 88

[tool.isort]
profile = "black"
